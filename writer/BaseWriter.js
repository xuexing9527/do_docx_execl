/**
 * 基类：写
 * 插入内容：insertContent
 * 写入内容到新文件：writeInNewFile
 * 根据type判断对谁（word/excel）写
 * 
 */
class BaseWriter {
    constructor (type) {
        this.type = type
    }

    // 插入内容
    insertContent () {

    }

    // 写入新文件
    writeInNewFile () {

    }
}